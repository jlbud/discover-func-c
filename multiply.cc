#include <iostream>
using namespace std;

int main()
{
    int firstNumber, secondNumber, result;
    cout << "Enter two number: ";
    cin >> firstNumber >> secondNumber;

    result = firstNumber * secondNumber;
    cout << "Tow number multiply is: " << result << endl;
    return 0;
}